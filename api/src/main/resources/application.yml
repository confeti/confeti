spring:
  server:
    port: ${PORT}

confeti:
  cluster:
    webUrl: ${WEB_URL}
  cassandra:
    local: false
    session:
      name: confeti_session
      keyspace: ${CASSANDRA_KEYSPACE_NAME}
      rf: 1
      durable: true
